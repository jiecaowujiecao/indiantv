/**
 * ============================================================
 *  NETPLAYA — SITE CONFIG
 *  Edit this file any time to change the site's key settings.
 *  No backend / admin panel needed — just change and re-deploy.
 * ============================================================
 */

/** The APK download link. Change this line whenever the link changes. */
export const APK_DOWNLOAD_URL = "https://jk2.anrtv.top/netplaya.apk";

/** Site / app name shown across the page. */
export const SITE_NAME = "NETPLAYA";

/** Public site URL (used for canonical + social preview tags). */
export const SITE_URL = "https://pixel-parade-441.lovable.app";

/** Meta (Facebook) Pixel ID. */
export const FB_PIXEL_ID = "1051934957414454";

/** App details shown in the "App Info" table. */
export const APP_INFO = {
  version: "V7.0.2 (2026)",
  size: "11 MB",
  platform: "Android 11+",
  downloads: "500,000+",
};
